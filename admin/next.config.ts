import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["res.cloudinary.com", "lh3.googleusercontent.com", "via.placeholder.com", "images.unsplash.com", "upload.wikimedia.org", "randomuser.me"]
  },
};

export default nextConfig;
